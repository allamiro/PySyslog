listen_port = 514
protocol = TCP

# Define how to process and store the logs
log_path = /var/log/pysyslog/

# Parsing rules for incoming logs
parse_patterns = /etc/pysyslog/patterns.conf

# Anomaly detection settings
anomaly_detection_enabled = yes
anomaly_threshold = 90